 CREATE TABLE IF NOT EXISTS territory (
      id PRIMARY KEY, 
      name TEXT,
      perimeter TEXT,
      description TEXT,
      types TEXT,
      user TEXT,
      created_at TEXT,
      updated_at TEXT,
      deleted_at TEXT
    );
