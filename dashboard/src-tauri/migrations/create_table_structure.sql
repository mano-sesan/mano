CREATE TABLE IF NOT EXISTS structure (
      id PRIMARY KEY, 
      name TEXT,
      phone TEXT,
      adresse TEXT,
      city TEXT,
      postcode TEXT,
      description TEXT,
      categories TEXT,
      created_at TEXT,
      updated_at TEXT,
      deleted_at TEXT
    );
